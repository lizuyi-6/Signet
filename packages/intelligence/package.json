{
  "name": "@signet/intelligence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Signet Intelligence Layer. Understands the semantic role page assets play (DOM-first heuristics + optional AI provider) and maps claims to evidence. Advisory/soft only — never enters the deterministic Trust Decision engine.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@signet/core": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
